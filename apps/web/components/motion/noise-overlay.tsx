/** Grão de filme fixo por cima de toda a página (textura cinematográfica). */
export function NoiseOverlay() {
  return <div aria-hidden className="noise pointer-events-none fixed inset-0 z-50" />
}
